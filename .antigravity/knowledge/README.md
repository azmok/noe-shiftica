# Knowledge Base

このフォルダはタスク完了ごとに蓄積される実装ナレッジの聖書。
AIエージェントは必ず以下のルールに従うこと。

---

## ディレクトリ構成
```
knowledge/
├── frontend/
│   ├── ui/        # コンポーネント・スタイル・レイアウト
│   └── logic/     # API呼び出し・状態管理・hooks
└── backend/
    ├── admin-ui/  # Payload Admin Panelカスタマイズ
    └── logic/     # コレクション定義・API routes・DB操作
```

---

## タスク完了時のルール（必須）

タスクが完了したら、必ずナレッジファイルを保存すること。

- **保存先**：内容に応じた適切なサブディレクトリ
- **ファイル名**：機能を表す英語のkebab-case（例：`user-avatar-upload.md`）
- **既存ファイルがあれば追記、なければ新規作成**

### 各ファイルの記載フォーマット

## [機能名]

### 実装パターン
どうやって実装したか、具体的に

### 使用ファイル
- `src/collections/Users.ts`

### 処理フロー
1. ステップ1
2. ステップ2

### ハマりポイント
次回のAIが同じ罠にハマらないための記録

---

## タスク開始時のルール（必須）

関連しそうなナレッジファイルを必ず事前に参照すること。
パスが不明な場合はこのREADMEのディレクトリ構成を起点に探すこと。