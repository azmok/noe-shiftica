services:
  postgres:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: azuma
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_DB: myblogdb
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
