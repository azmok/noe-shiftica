[O] safari でsource code パネル表示確認→全画面スクロール可能に
[O] safari でpreview 機能しない→　確認+修正
[O] 画像の読み込み、0.5秒以下に修正
[] ブランチを作って、スマホやiPadの時だけTopologyのThree.jsがスクロールアニメーションで表示されるように変更。
[O] HTMLインポーターで、スマホ表示・PC表示の両方とも記事枠全体に表示されるように修正

- `[x]` 1. Modify `useEffect` in `HearingPage` to initialize `currentStep` from URL and setup `popstate` listener.
- `[x]` 2. Update `changeStep` function to use `history.pushState` and `history.back()`.
- `[x]` 3. Update `handleOptionClick` to sync step changes with history.
- `[x]` 4. Final verification and cleanup.